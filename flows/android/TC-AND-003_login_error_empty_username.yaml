appId: com.saucelabs.mydemoapp.android
name: "TC-AND-003: Username field shows error when left empty"
tags:
  - android
  - auth
  - validation
onFlowStart:
  - takeScreenshot: TC-AND-003_start
onFlowComplete:
  - takeScreenshot: TC-AND-003_complete
---
- launchApp:
    clearState: true

- runFlow:
    file: ../subflows/login/navigate.yaml
    env:
      APP_ID: com.saucelabs.mydemoapp.android

# Leave both fields empty and tap Login
- tapOn: "Login"

- extendedWaitUntil:
    visible:
      text: "Username is required"
    timeout: 5000
